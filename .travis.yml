---
language: python
python: "3.6"
os: linux
dist: xenial
services:
  - docker
install:
  - pip install pipenv
  - pipenv sync
script:
   - pipenv run molecule test

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
